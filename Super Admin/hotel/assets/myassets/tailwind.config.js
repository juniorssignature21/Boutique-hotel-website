/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./**/*.html", // This will include all HTML files in the project directory and subdirectories
    "./**/*.js", // This will include all JS files in the project directory and subdirectories
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
